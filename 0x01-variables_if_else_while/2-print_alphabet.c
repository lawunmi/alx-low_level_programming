#include <stdio.h>
/**
 * main - prints alphabet in lowercase using putchar followed by a newline
 * Return: 0
 *
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
		putchar(a);
		putchar('\n');
	}
	return (0);
}
